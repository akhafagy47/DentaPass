# Supabase (auth only — no service key needed in website)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Backend API
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000   # client-side calls
BACKEND_URL=http://localhost:4000               # server-side (SSR) calls

# App
NEXT_PUBLIC_APP_URL=https://dentapass.ca
